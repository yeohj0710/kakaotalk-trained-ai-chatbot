# Optional Git LFS recommendation for published model artifact.
# Enable with:
#   git lfs install
#   git lfs track "artifacts/model_latest.pt"
#   git lfs track "artifacts/model_latest.enc"
artifacts/model_latest.pt filter=lfs diff=lfs merge=lfs -text
artifacts/model_latest.enc filter=lfs diff=lfs merge=lfs -text
