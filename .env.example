# Required for inference/chat/bridge gate
CHATBOT_PASSWORD=change-me

# Optional: store only hash instead of plaintext password
# CHATBOT_PASSWORD_HASH=<sha256_of_password>

# Required only when using encrypted model artifacts (.enc)
CHATBOT_MODEL_KEY=change-me-strong-key

# Optional defaults used by configs/gen.yaml
# CHATBOT_DEFAULT_BOT=최근용
# CHATBOT_DEFAULT_USER=김영찬

# Optional config override syntax:
# CHATBOT_CFG__train__optimization__learning_rate=0.0002
