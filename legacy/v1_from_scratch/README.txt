This folder contains archived files from the previous (pre-SFT) pipeline.

Archived items:
- old from-scratch training/inference modules
- old configs: train.yaml, gen.yaml
- old helper scripts: publish/encrypt/decrypt for the old flow

Current active pipeline:
- LoRA SFT with `python -m chatbot.sft_ops ...`
- main config: `configs/sft.yaml`
